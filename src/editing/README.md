# Parallel Multi-Strategy Editing System

Интеллектуальная система редактирования кода для Kimi VS Code Extension, вдохновлённая Codebuff но с глубокой интеграцией с VS Code.

## Особенности

### 🔄 Параллельное выполнение
- Запускает 3+ стратегии одновременно
- Использует prompt cache для эффективности
- Собирает результаты и выбирает лучший

### 🎯 Стратегии редактирования

| Стратегия | Риск | Описание |
|-----------|------|----------|
| 🛡️ Conservative | Low | Только необходимые изменения |
| ⚖️ Balanced | Medium | Оптимальный баланс |
| 🚀 Aggressive | High | Полный рефакторинг |
| 🧪 Test-First | Low | TDD подход |
| 🎯 Minimal Diff | Low | Минимальный diff |

### 🏆 Умный выбор результата
Оценка по критериям:
- Качество кода (30%)
- Эффективность diff (20%)
- Соответствие style guide (20%)
- Безопасность (20%)
- Уверенность модели (10%)

### 🔀 Smart Merge
- Объединяет непересекающиеся изменения
- Разрешает конфликты по приоритету
- Генерирует объяснение merge

### 🖥️ VS Code интеграция
- Side-by-side diff viewer
- Webview для выбора вариантов
- Inline decorations
- Status bar progress

## Быстрый старт

```typescript
import { 
  ParallelEditor, 
  createParallelEditor,
  VSCodeIntegrationImpl 
} from './editing';

// Создаём интеграцию с VS Code
const vscodeIntegration = new VSCodeIntegrationImpl();

// Создаём parallel editor
const editor = createParallelEditor(llmClient, vscodeIntegration, {
  defaultTimeout: 30000,
  mergeStrategy: 'smart',
});

// Контекст редактирования
const context: EditingContext = {
  filePath: '/path/to/file.ts',
  originalContent: sourceCode,
  language: 'typescript',
  projectContext: {
    styleGuide: '...',
    dependencies: ['...'],
  },
};

// Выполняем параллельное редактирование
const result = await editor.execute(context, "Fix the null pointer exception", {
  strategies: ['conservative', 'balanced', 'aggressive'],
  enableMerging: true,
  enableUserSelection: true,
});

// Применяем лучший результат
await vscodeIntegration.applyEdit(result.bestResult.content);
```

## Smart Edit

Автоматический выбор стратегий на основе запроса:

```typescript
const result = await editor.smartEdit(context, "Refactor this function");

// Bug fix -> ['minimal-diff', 'conservative', 'test-first']
// Refactor -> ['balanced', 'aggressive', 'conservative']
// Feature  -> ['test-first', 'balanced', 'aggressive']
// Optimize -> ['aggressive', 'balanced', 'conservative']
```

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    ParallelEditor                        │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │ Conservative│  │  Balanced   │  │ Aggressive  │     │
│  │  Strategy   │  │  Strategy   │  │  Strategy   │     │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘     │
│         │                │                │             │
│         └────────────────┼────────────────┘             │
│                          ▼                              │
│              ┌─────────────────────┐                    │
│              │    ResultSelector   │                    │
│              │  (rank & evaluate)  │                    │
│              └──────────┬──────────┘                    │
│                         ▼                               │
│              ┌─────────────────────┐                    │
│              │     DiffMerger      │                    │
│              │  (optional merge)   │                    │
│              └──────────┬──────────┘                    │
│                         ▼                               │
│              ┌─────────────────────┐                    │
│              │  VS Code Integration│                    │
│              │  (UI & user choice) │                    │
│              └─────────────────────┘                    │
└─────────────────────────────────────────────────────────┘
```

## API Reference

### ParallelEditor

#### `execute(context, request, options)`
Выполняет параллельное редактирование.

#### `quickEdit(context, request)`
Быстрое редактирование с одной стратегией (balanced).

#### `smartEdit(context, request)`
Умное редактирование с автовыбором стратегий.

### ResultSelector

#### `rankResults(results)`
Ранжирует результаты по оценке.

#### `selectBestResult(results)`
Выбирает лучший результат.

### DiffMerger

#### `mergeResults(original, results, strategy)`
Объединяет результаты.

#### `canMergeResults(result1, result2)`
Проверяет возможность merge.

### StrategyTemplates

#### `getStrategy(type)`
Получает стратегию по типу.

#### `createCustomStrategy(base, overrides)`
Создаёт кастомную стратегию.

## Конфигурация

```typescript
interface ParallelEditorConfig {
  defaultTimeout: number;           // 30000
  mergeStrategy: 'smart' | 'conservative' | 'aggressive';
  maxConcurrentStrategies: number;  // 5
  
  evaluatorConfig: {
    weights: {
      codeQuality: number;      // 0.3
      diffEfficiency: number;   // 0.2
      styleCompliance: number;  // 0.2
      safety: number;           // 0.2
      confidence: number;       // 0.1
    };
  };
}
```

## Преимущества над Codebuff

1. **VS Code интеграция**: Нативный UI, diff viewer, inline decorations
2. **Выбор пользователя**: Показываем 3+ варианта, пользователь решает
3. **Smart Merge**: Комбинируем лучшие части из разных стратегий
4. **Конфигурируемость**: Кастомные стратегии и веса оценки
5. **Кэширование**: Повторное использование prompt context

## Roadmap

- [ ] Интеграция с реальным LLM клиентом
- [ ] AST-based diff для более точного merge
- [ ] Обучение на feedback пользователя
- [ ] Предиктивный выбор стратегий
- [ ] Partial accept (выбор частей из разных стратегий)
